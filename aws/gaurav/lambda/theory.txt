## it Allows developers to focus on writing code without managing servers, making it easier to build microservices or event-driven architectures.

## Serverless computing service that runs code in response to events and automatically manages the computing resources required without managing server
